"""Request logger service — Phase 1 stub."""

from __future__ import annotations

# TODO(phase-2): Implement RequestLogger with:
#   - log(user_id, provider, model, strategy, tokens, cost, latency,
#          cache_hit, status, routing_decision, error_message) -> Request
#   Writes one row to the requests table after every completed inference.
