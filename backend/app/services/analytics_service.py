"""Analytics aggregation service — Phase 1 stub."""

from __future__ import annotations

# TODO(phase-2): Implement AnalyticsService with:
#   - get_overview(user_id, period_days) -> OverviewResponse
#   - get_cost_over_time(user_id, period_days) -> CostOverTimeResponse
#   - get_latency_over_time(user_id, period_days) -> LatencyOverTimeResponse
#   - get_provider_distribution(user_id, period_days) -> ProviderDistributionResponse
#   All queries are SQL aggregations on the requests table.
