"""
Authentication service — Phase 1 stub.

Handles user registration, login, and token issuance.
Full implementation in Phase 2 once the database layer is complete.
"""

from __future__ import annotations

# TODO(phase-2): Implement AuthService with:
#   - register(email, password, full_name) -> User
#   - login(email, password) -> TokenResponse
#   - get_user_by_id(user_id) -> User
#   - get_user_by_email(email) -> User | None
